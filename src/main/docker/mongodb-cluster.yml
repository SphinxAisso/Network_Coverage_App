version: '2'
services:
    cra-mongodb:
        image: mongo:3.2.10
        ports:
            - "27017:27017"
        command: mongos --configdb cra-mongodb-config
    cra-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    cra-mongodb-config:
        image: mongo:3.2.10
        container_name: cra-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
